# Optional: enables natural-language synthesis after retrieval.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Storage locations (relative to the repository root by default)
CHROMA_PATH=data/chroma
KNOWLEDGE_PATH=knowledge
